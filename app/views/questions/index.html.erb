<h1>All Questions</h1>

<% @questions.each do |question| %>
  <%= link_to question.title, question_path(question) %><span> &nbsp&nbsp&nbsp   </span>
  <%= pluralize question.likes.count, 'like' %>
  <hr />
<% end %>
